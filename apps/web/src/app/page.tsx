export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: 720 }}>
      <h1>WorkBrain</h1>
      <p>API backend is running. Phase 1 — paste ingestion + semantic search via MCP.</p>
    </main>
  );
}
